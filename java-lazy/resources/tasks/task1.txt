1. Создать любой класс с методом main

2. В этом методе создать переменные следующих типов:

3. int, long, float, double, boolean, char, String. При чем переменная типа double должна быть инициализированна значением 0б в последствии присвоено значени;
а переменная типа String инициализированна null, в последствии задано значение; переменная типа char объявлена без инициализации, в последствии задано значение.

4. Создать другие переменные типов int, long, float, double, при чем в каждую необходимо сохранить по не сколько значений разных типов (1, 2, 3, 4 соотвтетственно).
К примеру:
long myVar = 4;
myVar = 5L;